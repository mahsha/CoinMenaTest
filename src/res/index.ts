import color from "./color"
import font, { fontSize, fontHeight } from "./font"
import layout from "./layout"
import spacing from "./spacing"

export const R = Object.freeze({
  color,
  font,
  fontSize,
  layout,
  spacing,
  fontHeight,
})
